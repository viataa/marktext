// Cross-cutting ambient declarations for the renderer (window globals exposed
// by the preload contextBridge, build-time defines, and node-style globals
// that survived the sandbox migration).
//
// Populated in Commit 2 — kept as a placeholder here so tsconfig references
// resolve. See src/preload/index.ts for the canonical contextBridge surface.

export {}
